import { 
    // 
} from './types';

// ex syntax: 
/*
export const setVisibility = (array) => dispatch => {
    dispatch({
            type: SET_YEARS_VISIBILITIES,
            payload: array
    })
}
*/