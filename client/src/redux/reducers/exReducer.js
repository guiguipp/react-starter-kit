/*
import { 
    // Import Actions from type here,
    // EXAMPLE_ACTION,
} from "../actions/types"

const initialState = {
    // initial State
    user: undefined,
    }

export default function(state = initialState, action) {
    switch(action.type) {
        case EXAMPLE_ACTION:
        return {
            ...state,
            user: action.payload,
        }
        
        default: 
        return state;
    }
}
*/